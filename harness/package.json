{
	"name": "harness",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsup",
		"dev": "tsup --watch",
		"dev:server": "tsx src/index.ts",
		"test": "vitest",
		"test:run": "vitest run",
		"coverage": "vitest run --coverage",
		"lint": "biome check .",
		"lint:fix": "biome check --fix .",
		"format": "biome format --write ."
	},
	"dependencies": {
		"@openharness/contracts": "0.1.0",
		"@openharness/tempo": "0.1.0",
		"lowdb": "^7.0.1"
	},
	"devDependencies": {
		"zod": "^4.4.3"
	}
}
